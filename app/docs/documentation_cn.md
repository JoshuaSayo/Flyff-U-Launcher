## 受监督的视觉自动化（分支功能）

此社区分支包含一个受监督、仅基于画面的自动化工作台。默认模式仅观察；战斗模式需要明确确认，并且只控制当前选中的前台客户端。它不会使用游戏内存、数据包、DOM、官方 API 或插件数据。使用此功能可能违反游戏规则并给账号带来风险。启用前请阅读完整的[自动化指南](../../AUTOMATION.md)。

## 基本功能

:::accordion[创建档案]

**步骤 1 — 创建新档案：**
- 点击页眉中的 **"新档案"**。

![说明](create_profil/create_profil_1_de.png)

**步骤 2 — 输入档案名称：**
- 输入档案名称并点击 **"添加"**。
- 点击 **"关闭"** 可不创建档案直接关闭对话框。

![说明](create_profil/create_profil_2_de.png)

**步骤 3 — 了解档案卡片：**

每个档案在列表中以卡片形式显示：

![说明](create_profil/create_profil_3_de.png)

| 编号 | 元素 | 说明 |
|------|------|------|
| ❶ | 拖动手柄 | 通过拖放排序档案 |
| ❷ | Overlay 目标 | 确定哪个档案接收 OCR Overlay 和 Sidepanel |
| ❸ | 辅助目标 | 确定哪个档案作为 CD 计时器的辅助视图 |
| ❹ | 启动模式 | 显示档案以标签还是窗口模式打开 |
| ❺ | 齿轮图标 | 打开档案设置 |
| ❻ | 开始游戏 | 使用此档案启动游戏会话 |

**步骤 4 — 档案设置：**

点击齿轮图标 ❺ 打开设置：

![说明](create_profil/create_profil_4_de.png)

| 编号 | 元素 | 说明 |
|------|------|------|
| ❶ | 档案名称 | 修改档案名称 |
| ❷ | 职业 + 角色名 | 从下拉菜单选择职业并输入角色名。每个角色分配独立的职业。 |
| ❸ | 添加角色 | 向档案添加另一个角色名（点击"添加"按钮） |
| ❹ | 在标签中使用 | 开启：档案可在多标签布局中使用。关闭：档案仅在独立窗口中打开。 |
| ❺ | 保存 | 应用更改 |
| ❻ | 复制档案 | 创建包含所有设置的档案副本 |
| ❼ | 删除 | 永久删除档案 |
| ❽ | 关闭 | 关闭对话框 |

如需在标签和窗口两种模式下使用同一档案，请用 ❻ 复制，每种模式使用一个副本。

**步骤 5 — 带角色的档案列表：**

配置完成的档案在列表中显示角色名和职业图标：

![说明](create_profil/create_profil_5_de.png)

- 每个角色以带职业图标的徽章形式显示在档案名下方。
- 页眉的职业筛选和角色名搜索会搜索所有档案的所有角色。
- 插件（如 Killfeed）通过下拉列表使用已保存的角色名。

可以创建任意数量的档案。每个档案保存独立的 Flyff 会话。
游戏内设置不会像浏览器那样在会话之间共享。

**档案导出/导入：**

![small](create_profil/create_profil_6.png)

| 编号 | 元素 | 说明 |
|------|------|------|
| ❶ | 导出 | 将档案保存为 `.flyffprofile` 文件 |
| ❷ | 导入 | 加载 `.flyffprofile` 文件并创建新档案 |

导出的文件包含：

- 档案元数据（名称、职业、设置）
- Electron 会话 Cookie（登录数据）
- localStorage 数据（游戏设置）

可用于备份和电脑间转移。
:::

:::accordion[创建布局]

**步骤 1 — 启动布局：**

在启用了标签功能的档案上点击 **"开始游戏"**。

![说明](create_layout/create_layout_1_de.png)

**步骤 2 — 选择布局网格：**

选择所需的布局网格。悬停时右侧显示 **ASCII 预览**。

![说明](create_layout/create_layout_2.png)

*对称布局：*
- **1×1** — 单窗口
- **1×2 / 2×1** — 两个窗口并排 / 上下排列
- **1×3 / 3×1** — 三个窗口并排 / 上下排列
- **1×4 / 4×1** — 四个窗口并排 / 上下排列
- **2×2** — 四个窗口网格
- **3+2** — 上方三个，下方两个
- **2×3** — 六个窗口网格
- **4+3** — 上方四个，下方三个
- **2×4** — 八个窗口网格

*非对称布局：*
- **1+2 →** — 主窗口在左，2 个侧窗口在右堆叠
- **1+3 →** — 主窗口在左，3 个侧窗口在右堆叠
- **1+2 ↓** — 主窗口在上，2 个侧窗口在下并排
- **1+3 ↓** — 主窗口在上，3 个侧窗口在下并排

非对称布局在标签栏提供 **滑块**，可调整主窗口和侧窗口之间的分割比例（最小 20% / 最大 80%）。

![small](create_layout/create_layout_slider.png)

**步骤 3 — 分配档案：**

为每个格子分配档案。不需要的格子可留空。

![说明](create_layout/create_layout_3_de.png)

| 编号 | 元素 | 说明 |
|------|------|------|
| ❶ | 布局格子 | 显示所选网格的格子。点击格子可从下方列表分配档案。 |
| ❷ | 档案列表 | 所有启用标签的档案。点击可分配到选中的格子。 |
| ❸ | 下一步 | 确认分配并以分配的档案启动布局。 |

**步骤 4 — 保存布局：**

图片中标记的按钮（在标题栏中）打开保存对话框。

![说明](create_layout/create_layout_4.png)

输入布局名称并点击 **"保存"**。

![说明](create_layout/create_layout_5_de.png)

**步骤 5 — 启动器中的布局卡片：**

保存的布局在档案列表中显示为卡片：

![说明](create_layout/create_layout_6_de.png)

- 卡片显示 **布局名称**、**档案数量** 和 **网格缩略图**。
- 点击 **"开始游戏"** 启动整个布局。
- **齿轮图标** 打开布局设置（名称、档案分配、网格）。

**自定义布局（Custom）：**

除了预定义的网格外，**"自定义"** 选项允许创建自由布局。在编辑器中可以在画布上自由放置和调整 1–8 个单元格的大小。

![描述](custom_layout_editor.png)

| 编号 | 元素 | 描述 |
|-----|---------|-------------|
| ❶ | 添加单元格 | 添加新单元格（最多 8 个）。 |
| ❷ | 网格 | 移动/调整大小时的吸附精度（1%、5% 或 10%）。 |
| ❸ | 滑块 | 设置可调分割线：水平 (↔)、垂直 (↕) 或无 (—)。绿色线条可在编辑器中拖动，用于运行时调整分割。 |
| ❹ | 单元格 | 每个编号的单元格可以拖动移动，并通过角落和边缘的手柄调整大小。 |
| ❺ | 属性 | 所选单元格的 X/Y 位置和宽度/高度（百分比）。也可以直接输入数值。 |
| ❻ | 删除单元格 | 删除当前选中的单元格。 |

重叠的单元格将堆叠显示——顶部单元格接收输入。确认布局后，将像预定义网格一样进行配置文件分配。

**相关设置**（设置 / 布局）：
- **顺序加载网格标签** — 逐个启动标签而非同时启动
- **变更时更新布局** — 自动保存布局更改
- **突出显示活动网格视图** — 视觉上突出当前聚焦的标签
- **布局标签显示模式** — 启动器中布局标签的显示模式
- **布局延迟** — 切换标签时的延迟时间

**相关快捷键**（设置 / 快捷键）：
- **上一个标签** / **下一个标签** — 切换标签
- **下一个窗口** — 在打开的窗口间切换焦点
- **标签栏开/关** — 显示/隐藏会话窗口中的标签栏

**多窗口：**

除布局外还可并行打开多个独立会话窗口。在已有会话时打开档案会询问是添加到当前窗口还是创建新窗口。
:::

:::accordion[快捷键]

快捷键是可自由分配的组合键（2-3 个键），即使游戏窗口处于活动状态也能使用。

**配置：**
- 打开 **设置 → 快捷键**。
- 点击操作旁边的徽章，然后按下所需的组合键。
- 冲突会自动检测并显示。

![说明](hotkeys/hotkeys_settings_de.png)

**可用操作：**

| 操作 | 说明 |
|------|------|
| Overlay 开/关 | 显示或隐藏所有 Overlay |
| Sidepanel 开/关 | 打开或关闭 Sidepanel |
| 标签栏开/关 | 显示/隐藏会话窗口中的标签栏 |
| 上一个标签 | 切换到上一个标签 |
| 下一个标签 | 切换到下一个标签 |
| 下一个窗口 | 在打开的窗口间切换焦点 |
| CD 计时器归零 | 将所有 CD 计时器设为 00:00（等待按键） |
| 截图 | 保存活动窗口的截图 |
| FCoins 计算器 | 打开 FCoins 计算器 |
| 购物清单 | 打开 Premium 购物清单 |

配置好的快捷键可随时通过标签栏中的 **键盘图标** 查看。

![说明](hotkeys/hotkeys_menu_de.png)
:::

:::accordion[数据路径与持久化]

所有用户数据按平台存储在以下目录：

| 平台 | 路径 |
|------|------|
| **Windows** | `%APPDATA%\Flyff-U-Launcher\user\` |
| **macOS** | `~/Library/Application Support/Flyff-U-Launcher/user/` |
| **Linux** | `~/.config/Flyff-U-Launcher/user/` |

**重要文件和目录：**

| 功能 | 用途 | 相对路径 |
|------|------|----------|
| 档案 | 启动器档案（名称、职业、标记） | `user/profiles/profiles.json` |
| ROI 校准 | OCR/Killfeed 的 ROI 定义 | `user/profiles/rois.json` |
| OCR 计时器 | OCR 采样率 | `user/profiles/ocr-timers.json` |
| 布局 | 标签的网格布局 | `user/ui/tab-layouts.json` |
| 主题 | 用户主题 | `user/ui/themes.json` |
| 活动标签颜色 | 标签颜色设置 | `user/ui/tab-active-color.json` |
| 客户端设置 | 所有启动器设置 | `user/config/settings.json` |
| 功能开关 | 已激活的功能 | `user/config/features.json` |
| Premium 购物清单 | 每个道具的 FCoin 价格 | `user/shopping/item-prices.json` |
| 插件设置 | 各插件的设置 | `user/plugin-data/<pluginId>/settings.json` |
| Killfeed 历史 | 每个档案的每日概览 | `user/plugin-data/killfeed/history/<id>/history.csv` |
| Killfeed 单次击杀 | 按击杀和日期的详细历史 | `user/plugin-data/killfeed/history/<id>/daily/YYYY-MM-DD.csv` |
| API-Fetch 数据 | 插件所需的原始数据/图标 | `user/cache/` |
| 错误日志 | 诊断日志 | `user/logs/` |
| 升级计算器 | 保存的价格/设置 | `user/tools/upgrades/upgrade_cost_calc.json` |

:::

## 控制器

启动器及游戏均可完全通过手柄操控。

::youtube[fSYgnEh36-g]

按键绑定**按配置文件**单独保存，每个角色可拥有独立的布局。


:::accordion[自定义按键绑定]

打开**设置 → 控制器**。

- 在顶部的**配置文件**下选择要编辑绑定的角色。
- 按键按组分类：**面按钮**、**肩键 & 扳机**、**方向键**和**系统 & 摇杆**。
- 点击某个按钮旁的**设置按键**，然后按下所需按键 — 该按键将被保存为目标输入。
- **取消绑定**可移除一项绑定，**默认**将单个按钮重置，**全部重置**则重置整个配置文件。
- 在控制器示意图中，可点击或悬停某个按钮，直接跳转到其绑定设置。
- 点击**保存**应用绑定。

摇杆功能固定不变：**左摇杆 = 移动**，**右摇杆 = 相机**。
:::


:::accordion[特殊操作]

一个按钮除普通按键外，还可以触发**特殊操作**。

| 操作 | 效果 |
|------|------|
| Action-Pad 触发 | 点击游戏 HUD 中的 Action 按钮（攻击/跳跃/互动） |
| 放大 / 缩小 | 拉近 / 拉远镜头 |
| 上一个标签 / 下一个标签 | 在已打开的配置文件标签间切换 |
| 重新加载视图 | 重新加载当前游戏窗口 |
| 切换全屏 | 将启动器窗口切换为全屏 |
| 打开设置 | 打开设置窗口 |
| Ringmaster（转发） | 输入转发给 Ringmaster |
| 点击槽位 1 – 8 | 点击已校准的队伍成员（参见"锁定队伍成员"） |

:::

:::accordion[Action-Pad]

攻击需要使用 D-Pad。

**步骤 1：** 在游戏中启用：菜单 → 选项 → 游戏 → "Directional Pad" **Action-Pad**。

**步骤 2：** 将一个按钮（例如 X）绑定到特殊操作 **Action-Pad**。

**步骤 3：** 在游戏中按 **Ctrl + Shift + F1** 开始校准。

**步骤 4：** 在游戏中的下一次点击将确定 Action-Pad 的锚点。点击 Action 按钮的中心位置。

**结果：** 只要 Action-Pad 可见，即可用所选按钮攻击附近的怪物。

:::

:::accordion[修饰键层]

通过**修饰键层**可实现更多按键绑定。启用后，按住对应肩键时，面按钮（PlayStation 上为 △ ◯ ✕ ☐）可设置多套绑定。

- 在控制器标签中打开**修饰键层**部分。
- 将某个肩键启用为修饰键（开关切换为**开**）。
- 为每个面按钮设置替代操作。"（默认）"将继续使用默认绑定。

:::



:::accordion[Ringmaster 转发]

通过 **Ringmaster 转发**，可在不切换可见窗口的情况下控制后台标签中的 Ringmaster。

**步骤 1：** 在控制器标签的主设置中，于 **Ringmaster 目标**下选择接收转发输入的配置文件。

**步骤 2：** 将特殊操作 **Ringmaster（转发）**绑定到一个按钮。

**步骤 3：** 在游戏中按住转发键 → 所有输入将发送给 Ringmaster。松开 → 输入返回主角色。

**注意：** 目标配置文件必须已打开并已登录。若未满足条件，转发的技能将无效触发。

:::

:::accordion[锁定队伍成员]

通过**名字槽位 1–8** 可将锁定队伍成员的操作绑定到按钮上。

**步骤 1：** 在控制器标签中，例如在 Ringmaster 配置文件上选择**校准槽位 1**。

**步骤 2：** 在游戏中打开队伍窗口（可使用紧凑版本），点击队伍中的第一个名字。

**步骤 3：** 将特殊操作"点击槽位 1"分配给一个按钮。



:::


:::accordion[技能图标选择器]

为了一眼看出哪个技能对应哪个面按钮，可以为每个按钮（✕ ◯ △ ☐）指定一个图标。

- 在控制器标签中，点击对应按钮旁的**选择技能图标**（相机图标）。
- 在选择器中搜索图标，或通过**全部 / 技能 / 物品 / 增益**标签进行筛选。
- 点击图标即可应用；**Shift + 点击**按钮可移除图标。

**提示：** 图标选择器访问图标缓存。显示图标需要 **API-Fetch**（技能，视需求也包括物品）和 **CD-Timer**。

面按钮预览可通过 **Ctrl + Shift** 拖动移位。

:::

## 插件

插件通常需要来自 API 的数据和图标，可通过 API-Fetch 下载。

:::accordion[API-Fetch]

API-Fetch 从 Flyff Universe API 下载数据和图标。其他插件（Killfeed、CD 计时器、Quest Guide、Premium 购物清单）需要这些数据。

- 在设置菜单或 Sidepanel 中打开 **"API-Fetch"**。
![说明](api_fetch/api_fetch_1.png)

- 选择所需的端点并点击 **"Start"**。
![说明](api_fetch/api_fetch_2.png)

进度可实时查看。状态显示哪些端点已处理完成。
由于 API 限制，会有短暂暂停以遵守速率限制。
![说明](api_fetch/api_fetch_3.png)

API-Fetch 也可在 Sidepanel 中使用。
![说明](api_fetch/api_fetch_4.png)

**可用端点：**

| 端点 | 被以下插件使用 |
|------|----------------|
| **Monster** | Killfeed、Giant Tracker |
| **Item** | CD 计时器、Premium 购物清单、Quest Guide |
| **Skill** | CD 计时器 |
| **Quest** | Quest Guide |
| **NPC** | Quest Guide |

:::

:::accordion[CD 计时器]
- 追踪技能/道具的冷却时间。计时结束时，带红色边框的图标会提示按下对应按键。
- 显示图标所需的 API-Fetch："Item" + "Skill"。

- 确认已启用 CD 计时器。
![说明](cd_timer/cd_timer_1_de.png)

- Sidepanel 中会出现 CD 计时器标签：
![说明](cd_timer/cd_timer_2_de.png)
- "0/0 aktiv"显示已配置和已激活的计时器数量。
- 勾选"Alle aktiv"复选框可激活全部计时器。
- "Alle abgelaufen"按钮将所有计时器重置为 0:00:00，
  即等待配置的按键输入。

- 计时器图标的显示可配置：X 和 Y 位置、图标大小以及列数。

- 点击"+"可创建新计时器。

- ![说明](cd_timer/cd_timer_3_de.png)
- 复选框启用此计时器。
- 点击"Icon"按钮打开图标选择对话框。
- 文本输入框中的内容会显示在图标上。
  提示：写入需要按的按键，例如"F1"。
- 设定时间和快捷键后，还可选择目标。
  主视图（启动器中的剑图标）或辅助视图（启动器中的法杖图标）。
 此设置决定在哪个窗口中等待按键。
  图标始终显示在主视图窗口中。
 因此你可以为 RM Buff 设置计时器，并在主视图中显示需要刷新的提示。


- ![说明](cd_timer/cd_timer_4_de.png)

- 指向辅助视图的计时器会有橙色光晕以示区分。


- ![说明](cd_timer/cd_timer_5_de.png)
:::

:::accordion[Killfeed]
- 通过 OCR 系统实时追踪击杀和经验值（EXP）。
- 显示怪物数据所需的 API-Fetch："Monster"

**功能：**
- 通过 OCR 识别击杀（自动检测 EXP 变化）
- 会话和总计统计（击杀数、EXP、击杀/小时、EXP/小时等）
- Overlay 徽章直接显示在游戏窗口中

**提示：**
- 目前 Killfeed 仅支持 1v1 升级。
- 切换角色时可能会出现混淆。

**设置：**

1. **如未完成：下载 API 数据**
   - 打开插件 [API-Fetch](action:openPlugin:api-fetch)，确保选择了端点 **"Monster"**。
   - 开始下载。需要怪物数据来按经验表验证击杀。
     （见 API-Fetch 文档）
2. **启用插件**
   - 在启动器中打开插件设置，确保 **Killfeed** 已启用。

3. **校准 OCR 区域**（每个档案一次）
   - 通过启动器按下激活的"剑按钮"启动游戏窗口。
   - 在 Sidepanel 中打开 ROI 校准（感兴趣区域）。
   - 在游戏中框选以下显示区域：
     - **EXP%** — 经验值显示
     - **Level** — 等级显示
     - **角色名** — 角色的名称
   - 保存区域。每个档案单独保存，只需设置一次。
   - 左键拖动 ROI。
   - 设置一个 ROI 后可按 TAB 选择下一个。
   - Killfeed 需要设置：LVL、NAME、EXP、ENEMY（敌方等级）、ENEMY HP
   - 按"关闭"或 ESC 完成 ROI 输入。
   - ROI 可在绘制后微调。
   - 识别结果可在 Sidepanel 中实时查看。
   - 最重要的是 LVL 和 EXP；ENEMY 和 ENEMY HP 目前仅起辅助作用，未来会更加重要。
   - 若实时 OCR 的等级显示不正确，可手动设置，
    手动设置的值优先于 OCR 值。
   - 若 OCR 偶尔误读 EXP 值（如切换角色时），可手动重新设置。
     EXP 规则可能会阻止自动纠正。



4. **在 Sidepanel 中选择档案**
   - 打开 Sidepanel，选择 **Killfeed** 标签。
   - 在下拉列表中选择要追踪的档案。
   - 角色名通过下拉列表从档案中选择——无需手动输入。


5. **开始游戏**
   - 击杀怪物后，OCR 系统会检测 EXP 变化。
   - 击杀和统计会自动显示在 Overlay 和 Sidepanel 中。

**Sidepanel：**
- 可切换单个徽章的显示（如 每会话击杀数、EXP/小时、升级所需击杀数）。
- 调整 Overlay 缩放比例（0.6x – 1.6x）。
- 选择徽章跨越的行数。
- Overlay 可通过拖拽移动到游戏窗口中的任意位置。位置会被保存。
- 用重置按钮清空会话统计。
- 每个会话的数据都会本地保存在你的电脑上。



- 每次识别到的击杀都会显示在 Sidepanel 中，并持久保存。
- 数据按档案分别写入 AppData 下的 CSV 文件：
  - `user/plugin-data/killfeed/history/<profile-id>/daily/YYYY-MM-DD.csv`（单次击杀明细）
  - `user/plugin-data/killfeed/history/<profile-id>/history.csv`（每日概览）
- 在怪物追踪折叠面板中，每个阶级都有一个 `Kills` 按钮。
- `Kills` 打开所选阶级的单次击杀列表视图。
-

- 在列表视图中可以删除单次击杀记录（`删除` -> `确认`）。
- 删除后会直接更新 Sidepanel 显示，以及 Killfeed 历史文件（`daily/YYYY-MM-DD.csv` 和 `history.csv`）。



**击杀判定规则：**
满足以下全部条件才计为一次击杀：
- 等级未变化（无升级/降级）。
- EXP 增加超过 0.001%（Epsilon 阈值）。
- EXP 跳变不超过 40%（Suspect 阈值）。更大跳变视为可疑并丢弃。
- 最近 1500 ms 内检测到敌方血条（OCR）。或者：无血条时，与上次击杀间隔至少 2250 ms 也可接受。
- 若存在 API-Fetch 的怪物数据：EXP 增益需介于怪物经验表预期值的 10% 与 10 倍之间。超出范围的值视为 OCR 误差并丢弃。

**被拒绝的 EXP 变化：**
- 升级或降级：不计击杀。
- EXP 下降：忽略（OCR 噪声）。
- EXP 跳变超过 40%：标记可疑，不计入。
- 无血条且距上次击杀少于 2250 ms：不计击杀。

**提示：**
- 必须启用 OCR 系统才能识别击杀。
- 击杀/小时等统计按滚动的 5 分钟窗口计算。
:::

:::accordion[Killfeed：Giant Tracker]
# 注意：
## 在首次记录到 Giant、Violet 或 Boss 击杀之前，会显示示例数据来演示该功能
---
Giant Tracker 是 Killfeed 插件中的独立窗口。它追踪并可视化 **Giants**、**Violets** 和 **Bosses** 的击杀统计——包括时间范围、掉落物和击杀时间（TTK）。五个筛选标签（全部、Giants、Violets、Bosses、Drops）支持按阶级或已记录的掉落进行针对性筛选。

**打开方式：**
- 在 Killfeed Sidepanel 中有 **"Giant Tracker"** 按钮。
- 点击后会打开一个独立窗口，显示所有追踪的 Boss 怪物概览。
- 如果还没有真实击杀数据，将显示示例数据。

![说明](killfeed_giant_tracker/killfeed_giant_tracker_1_de.png)

---

**筛选和排序：**
- 筛选栏可以限制显示范围：
  - **全部** / **Giants** / **Violets** / **Bosses** / **Drops** ——按怪物阶级或掉落物筛选。
  - **Bosses** ——仅显示阶级为 `boss` 的怪物（如 Clockworks、Meteonyker）。Boss 卡片有红色边框。
  - **Drops** ——仅显示至少有一条掉落记录的怪物。此外，卡片中直接显示掉落池预览（按稀有度排列的前 5 个物品）。
  - **排序** ——按击杀数（升序/降序）、名称（A–Z / Z–A）或等级（升序/降序）。
  - **搜索框** ——按怪物名称筛选卡片。

![说明](killfeed_giant_tracker/killfeed_giant_tracker_2_de.png)

---

**卡片视图：**

每个追踪的怪物显示为一张卡片。有两种视图：

*紧凑卡片（默认视图）：*
- 怪物图标、名称、等级、属性、阶级
- 战斗数值（HP、ATK）
- 击杀概览：今天 / 总计
- TTK 显示（如有测量数据）：`TTK: 45.2s (平均 52.3s)`
- 最后击杀（时间）、掉落数量
- **"详情"** 按钮展开

![说明](killfeed_giant_tracker/killfeed_giant_tracker_3_de.png)

*展开卡片（详细视图）：*
- 紧凑卡片的所有字段
- 按时间段的击杀统计：今天、本周、本月、今年、总计
- TTK 统计：平均 TTK、最后 TTK、最快
- 掉落区域：掉落数量、平均击杀/掉落、自上次掉落以来的击杀数
- 掉落历史（可折叠）：包含物品名称、击杀计数器和时间戳的单个掉落记录
- **"记录掉落"** 按钮记录掉落
- **"收起"** 按钮关闭详细视图

![说明](killfeed_giant_tracker/killfeed_giant_tracker_4_de.png)

---

**掉落追踪：**

展开卡片中的 **"记录掉落"** 按钮会打开一个对话框：
- 显示怪物的掉落池（如果已通过 API-Fetch 下载怪物数据）。
- 物品可以按名称搜索并按稀有度筛选（普通、不常见、稀有、非常稀有、唯一、究极）。
- 点击物品会记录掉落，附带当前时间戳和击杀计数。
- 之前记录的掉落可以从掉落历史中逐个删除。

![说明](killfeed_giant_tracker/killfeed_giant_tracker_5_de.png)
![说明](killfeed_giant_tracker/killfeed_giant_tracker_6_de.png)

---

**击杀时间（TTK）：**

TTK 自动测量与 Boss 怪物的战斗时间——从第一次打击到击杀。

*工作原理：*
- **开始：** 检测到敌方 HP 条 `当前 < 最大`（战斗开始）。
- **停止：** 通过 EXP 检测确认击杀。累积的战斗时间被保存。
- **暂停：** HP 条消失（如取消选择目标进行加 Buff 或治疗）。开始 10 秒的宽限期。
- **继续：** 如果在 10 秒宽限期内重新选择同一 Boss 怪物，计时器继续。暂停时间不计入 TTK。
- **中止：** 如果宽限期结束仍未重新选择 Boss，TTK 测量被丢弃。

*目标识别：*
- 战斗开始时保存怪物名称和最大 HP。
- 重新选择时比较名称和最大 HP——只有匹配时计时器才会继续。
- 如果选择了不同的 Boss 怪物，当前测量被中止并开始新的测量。
- 如果选择了普通怪物，Boss 计时器暂停；普通击杀继续计数。

*显示和统计：*
- 紧凑卡片：`TTK: [最后击杀] (平均 [平均值])`
- 展开卡片：平均 TTK、最后 TTK、最快
- TTK 值按击杀保存在 CSV 历史中（`TTK_ms` 列），并按怪物汇总。

*限制：*
- TTK 测量仅对 Giants、Violets 和 Bosses 有效。普通怪物不被测量。
- 精度取决于 OCR 采样率（通常：每 500-1000 毫秒）。

---

**数据来源：**
- 击杀数据来自 Killfeed CSV 历史（`daily/YYYY-MM-DD.csv`）。
- 掉落日志按档案分别存储。
- 怪物详情（图标、HP、ATK、掉落池）来自通过 API-Fetch 下载的怪物数据。

:::


:::accordion[Quest Guide]
- 显示按等级、区域和类型筛选的可用任务——带有任务链可视化和每个档案的进度追踪。
- 所需 API-Fetch：**Quest**、**NPC**、**Monster**、**Item**

**设置：**
1. 确认插件 **Quest Guide** 已启用。
2. 通过 API-Fetch 下载所需的 API 数据（Quest、NPC、Monster、Item）。
3. 在 Sidepanel 中选择 **Quest Guide** 标签。

**筛选与搜索：**
- **搜索框** — 按任务名称、NPC 或道具筛选
- **等级模式：**
  - *OCR ±* — 显示与 OCR 当前识别等级匹配的任务（可调容差，默认：±5）
  - *手动* — 手动输入等级和容差
  - *最小-最大* — 设定固定等级范围（默认：1-30）
- **区域** — 限制为特定游戏区域
- **类型筛选** — 全部 / Chain / Daily / Repeat / Category
- **子类别** — 重复任务中：宠物、收集、怪物猎杀、递送、其他

**进度追踪：**
- 将任务标记为已完成——进度按档案保存
- 复选框"显示已完成"显示/隐藏
- 复选框"显示不可用"显示/隐藏
- 重置按钮重置进度

**统计栏：**
一目了然地显示总任务数、可用任务数和已完成任务数。

**任务地图：**
- 通过 Sidepanel 中的地图按钮打开带有任务位置的交互式地图。

![说明](quest_guide/quest_guide_sidepanel_de.png)
![说明](quest_guide/quest_guide_map_de.png)
:::

## 工具

工具可通过快捷键或标签栏中的菜单（星形图标）打开。

:::accordion[Fcoin <-> Penya]

![说明](tools/fcoin_zu_penya/fcoin_zu_penya_1.png)
- 将 FCoins 与 Penya 互相换算。
- 输入当前每 FCoin 的 Penya 汇率。汇率会保存，下次打开时自动加载。
- 修改 FCoin 数量或 Penya 结果——计算双向自动更新。

![说明](tools/fcoin_zu_penya/fcoin_zu_penya_2.png)

:::

:::accordion[Premium 购物清单]
- Premium 商店购物规划工具；方便在购买 FCoin 前估算需求。需允许弹窗。
- 前提条件：通过 API-Fetch 端点 **"Item"** 加载图标；否则搜索为空。
![说明](tools/premium_shopping_list/premium_shopping_list_1.png)
- 用法：
  1. 在菜单（星形图标）中打开工具，在搜索框中输入道具名称。
  2. 结果列表（最多 20 条）显示图标、名称和类别；用 **"+ Add"** 添加或增加数量。
  ![说明](tools/premium_shopping_list/premium_shopping_list_2.png)
  3. 在列表中设置每个道具的价格（FCoins）和数量；离开输入框时价格会保存，并在后续搜索中预填。
  4. 复选框标记已完成/已购买的道具，"X"删除条目。
  5. 底部进度条显示所有条目的总和（`价格 × 数量`），单位为 FCoins。
- 存储：价格持久保存在启动器数据目录中；列表本身每次会话重新开始。

:::

:::accordion[升级计算器]

所有升级类型集成在一个窗口中，使用侧栏导航。计算预期费用、材料数量和尝试次数。

![说明](tools/upgrade_cost_calc/upgrade_cost_calc_1.png)

**侧栏分区：**

| 分区 | 骰子 | 保护 |
|------|------|------|
| **武器 / 护甲 / 盾牌** | Powerdice 4/6 或 12 | S-Protect / Low S-Protect |
| **饰品** | Dice 8 | A-Protect |
| **护甲穿透** | Dice 8 | G-Protect |
| **武器/盾牌穿透** | Dice 8 | G-Protect |
| **Ultimate 武器** | – | Ultimate Orb + XProtect |
| **Ultimate 饰品** | – | Ultimate Orb + XProtect |

**设置（武器 / 护甲）：**

- **骰子类型：** Powerdice 4/6（标准）或 Powerdice 12（更高成功率）
- **从等级 / 到等级：** 设定升级范围（如 +3 → +7）
- **模式：**
  - **对比** — 并排显示两种保护系统的费用
  - **SProtect** — 使用普通 S-Protect 卷轴计算
  - **Low SProtect** — 使用更便宜的 Low SProtect 卷轴计算

**材料价格：**

在"材料"下可以设置当前的市场价格。勾选"已拥有"可将材料从费用计算中排除。

![说明](tools/upgrade_cost_calc/upgrade_cost_calc_2.png)

**结果：**

点击"计算"后，会显示每个升级等级的详细表格：

| 列 | 含义 |
|----|------|
| 等级 | 目标升级等级 |
| 概率 | 成功率（百分比） |
| 尝试次数 | 预期尝试次数 |
| 矿物 | 所需矿物 |
| Eron | 所需 Eron |
| Penya | Penya 费用 |
| 保护卷轴 | 所需保护卷轴 |
| 总费用 | 所有费用的 Penya 总和 |

![说明](tools/upgrade_cost_calc/upgrade_cost_calc_3.png)

在对比模式下，两种保护系统并排显示——更便宜的选项以绿色高亮。

**存储：** 价格和设置自动保存。

:::

## 其他

:::accordion[公告]

启动器右侧面板显示开发者的消息——无需更新应用。例如：已知错误、当前开发进展或计划中的功能。显示语言为德语和英语，可在设置中关闭。

![说明](announcements/announcements_de.png)
:::

:::accordion[消息和错误日志（Logs）]

通过标签栏中的 **Log 图标** 打开一个独立的错误日志窗口。

**功能：**
- 显示所有警告和错误消息，带时间戳：`[HH:MM:SS] [LEVEL] [MODULE] 消息`
- **删除** — 清除所有日志条目
- **保存** — 将日志导出为 `.txt` 文件（保存在 `user/logs/` 下）
- **发送消息** — 将日志直接发送给开发者（Discord）
  - 可选：添加描述和游戏内/Discord 名称
  - 60 秒冷却时间，防止意外重复发送

![说明](logs/logs_window_de.png)
:::

:::accordion[更新检查和版本回退]

**自动更新：**
- 启动器在启动时检查是否有新版本（可在设置中配置）。
- 如有更新可用，会显示带下载选项的对话框。
- 下载过程中显示进度条。
- 下载完成后，更新将在下次重启时安装。

**手动检查：**
- 在 **设置 → 行为** 中有一个 **"立即检查"** 按钮。

**版本回退：**
- 可从设置中直接安装旧版启动器（3.0.5 起）。
- 下拉列表显示所有可用的 GitHub Release，带日期和当前版本标记。
- 选择版本后将下载并在重启时安装。

![说明](settings/settings_update_de.png)
:::

:::accordion[RAM 显示]

在 **设置 → 显示 → "显示 RAM 使用量"** 中可在标签栏启用内存显示。

**功能：**
- 显示总内存消耗（MB）。
- 点击可打开详细分类：
  - 每个档案的内存消耗
  - 插件的内存消耗（估计值，因为是共享的）
  - 系统开销（启动器 + OCR）

![说明](ram/ram_display_de.png)
:::
